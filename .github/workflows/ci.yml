name: Python-Flask-CI
on:
  push:
    branches: [ main ]
    
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: take the code
        uses: actions/checkout@v7

      - name: install the linetr to check the syntax
        run: pip install ruff

      - name: use linter to check the python code
        run: ruff check .
      
      - name: Log in to Docker Hub to push the image
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image into the docker hub
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/my-app:latest

  CD:
    runs-on: ubuntu-latest
    needs: CI
    steps:
      - name: deploy the code
        run: echo deployed the app successfully
      # - name: Deploy the Docker image to the server
      #   uses: appleboy/ssh-action@v0.1.6
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USERNAME }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       docker pull ${{ secrets.DOCKERHUB_USERNAME }}/my-app:latest
      #       docker stop my-app || true
      #       docker rm my-app || true
      #       docker run -d --name my-app -p 80:80 ${{ secrets.DOCKERHUB_USERNAME }}/my-app:latest
      























# This is a basic workflow to help you get started with Actions


# name: linting
# on:
#   push:
#     branches: main
#     paths: "*.py"

# jobs:
#   Code-Test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: clone the code   # Clone the code from the repo into this runner
#         uses: actions/checkout@v7

#       - name: install linter
#         run: pip install ruff
       
#       - name: check the files for lining
#         run: ruff check .

#   Build:
#     runs-on: ubuntu-latest
#     needs: Code-Test
#     steps:
#       - name: Build the app
#         run: echo "Build successfully"

#   Deploy:
#     runs-on: ubuntu-latest
#     needs: Build
#     steps:
#       - name: deploy the code
#         run: echo "Deployed successfully"
